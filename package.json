{
  "name": "photo-timeline",
  "repository": "",
  "license": "GPL-3.0-or-later",
  "bin": {
    "ingest": "./bin/ingest.ts",
    "reindex": "./bin/reindex.ts"
  },
  "scripts": {
    "start": "cd webapp && vite",
    "build:webapp": "cd webapp && vite build",
    "deploy": "npm run build && rm -rf /home/christian/Data/MyMedia/Photos/_app_ && cp -r build /home/christian/Data/MyMedia/Photos/_app_",
    "tsc": "tsc --build lib server && tsc -p webapp",
    "build": "npm run build:webapp && npm run build:server",
    "test": "npm -w lib run test",
    "test:ci": "npm -w lib run test:ci",
    "--server--": "",
    "server": "PHOTOS=/home/christian/TestMedia/Photos ts-node-dev -T -P server/tsconfig.json server/index.ts",
    "build:server": "esbuild --bundle --minify --keep-names --target=node18 --platform=node --outfile=build/server.js server/index.ts"
  },
  "dependencies": {
    "@koa/cors": "^4.0.0",
    "@preact/signals": "^1.1.3",
    "date-fns": "^2.21.1",
    "fs-extra": "^11.1.1",
    "koa": "^2.13.1",
    "koa-bodyparser": "^4.3.0",
    "koa-static": "^5.0.0",
    "preact": "^10.13.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.5.0",
    "@testing-library/preact": "^3.2.3",
    "@tsconfig/node18": "^1.0.0",
    "@types/fs-extra": "^11.0.1",
    "@types/jest": "^29.5.0",
    "@types/koa": "^2.13.1",
    "@types/koa__cors": "^3.0.2",
    "@types/koa-bodyparser": "^4.3.0",
    "@types/koa-static": "^4.0.1",
    "esbuild": "^0.17.16",
    "identity-obj-proxy": "^3.0.0",
    "prettier": "^2.8.8",
    "ts-node": "^10.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.4",
    "vite": "^4.2.0"
  },
  "workspaces": [
    "lib"
  ]
}
