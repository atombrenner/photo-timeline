{
  "name": "server",
  "description": "serve photos and manipulate index",
  "scripts": {
    "tsc": "tsc",
    "start": "ts-node-dev -T index.ts",
    "prebuild": "rm -rf build",
    "build": "esbuild --bundle --minify --keep-names --target=node20 --platform=node --outfile=./build/server.js index.ts"
  },
  "dependencies": {
    "@koa/cors": "^4.0.0",
    "date-fns": "^2.21.1",
    "fs-extra": "^11.1.1",
    "koa": "^2.13.1",
    "koa-bodyparser": "^4.3.0",
    "koa-cors": "^0.0.16",
    "koa-static": "^5.0.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.1",
    "@types/koa": "^2.13.1",
    "@types/koa__cors": "^4.0.0",
    "@types/koa-bodyparser": "^4.3.0",
    "@types/koa-static": "^4.0.1",
    "@types/node": "^20.2.5"
  }
}
