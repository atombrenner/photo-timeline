{
  "name": "lib",
  "scripts": {
    "test": "vitest --globals --reporter verbose",
    "test:ci": "vitest --run --globals --reporter basic --reporter junit --outputFile junit.xml",
    "ffprobe": "ts-node -T ffprobe.ts"
  },
  "dependencies": {
    "@atombrenner/exif-reader-jpeg": "^2.0.0",
    "date-fns": "^3.0.6",
    "fs-extra": "^11.1.1"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.1"
  }
}
